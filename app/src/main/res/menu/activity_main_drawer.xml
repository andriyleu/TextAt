<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/myMarks"
            android:icon="@drawable/ic_mymarks"
            android:title="@string/myMarks" />
        <item
            android:id="@+id/myMentions"
            android:icon="@drawable/ic_mention"
            android:title="@string/myMentions" />
    </group>

    <item android:title="Opciones">
        <menu>
            <item
                android:id="@+id/settings"
                android:icon="@drawable/ic_menu_manage"
                android:title="Ajustes" />
            <item
                android:id="@+id/logout"
                android:icon="@drawable/ic_logout"
                android:title="Salir" />
        </menu>
    </item>

</menu>
